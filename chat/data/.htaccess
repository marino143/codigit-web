# Nitko ne smije direktno pristupiti bazi i uploadanim datotekama —
# sve se poslužuje isključivo kroz media.php uz provjeru prijave.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
